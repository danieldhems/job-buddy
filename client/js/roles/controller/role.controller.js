var RoleCollection = require('../collection/role.collection.js');
var RoleModel = require('../model/role.model.js');

var controller = (function(collection, model){
	// Add role
	
	// Edit role

	// Remove role

	// Add spec

	// Get spec

	// 
}(RoleCollection, RoleModel);

module.exports = Window.JobBuddy.Controllers.Role = controller;